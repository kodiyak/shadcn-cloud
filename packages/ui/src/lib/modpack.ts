import * as cva from 'class-variance-authority';
import * as clsx from 'clsx';
import * as twMerge from 'tailwind-merge';

export const modpackUi = {
	clsx,
	'class-variance-authority': cva,
	'tailwind-merge': twMerge,
};
